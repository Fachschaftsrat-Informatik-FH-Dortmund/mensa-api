# Go build output
/bin/
# `go build ./...` drops the binary right here, next to the source
/mensa-api

# pixi environment, reproducible via `pixi install`
/.pixi/

# Server-Konfiguration und Einrichtungsanleitung: nennen Benutzer, Pfade und
# sudo-Regeln, die in ein öffentliches Repository nicht gehören.
/deploy/
